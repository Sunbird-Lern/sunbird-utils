CREATE TABLE IF NOT EXISTS sunbird.content_badge_association(id text, contentid text, badgeid text, issuerid text, badgeclassimage text, badgeclassname text, createdon timestamp,
 status boolean, lastupdatedon timestamp, createdby text, lastupdatedby text, PRIMARY KEY(id));
