{
    cache = {
        mapNames = "getPageData,getSection,getSystemSetting,pageAssemble",
        ttl = {
            pageAssemble = 600,
            getSystemSetting = 600,
            getPageData = 600,
            getSection = 600
        }
    }
}
